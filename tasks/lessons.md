# 教訓・トラブルシューティング (`tasks/lessons.md`)

## 1. SwiftBar / xbar のプラグイン読み込み不具合

### 事象
`brew install --cask swiftbar` による自動インストール後、単に `open -a SwiftBar` で起動するだけでは、メニューバーにプラグインが表示されず「SwiftBar」という文字だけが表示され、裏でプラグインディレクトリ選択画面（またはルート `/` を指した状態）で待機してしまう。

### 原因
SwiftBar の初回起動時のデフォルト設定（plist）で、`PluginDirectory` が `/` や未設定になっており、我々が作成した `~/Library/Application Support/SwiftBar/plugins/` を自動認識せずにロードを停止していた。

### 対策・解決手順
`defaults` コマンドを使用して、plist に直接正しいプラグインパスを書き込み、アプリを強制再起動することでダイアログなしで一撃ロードさせることが可能。

```bash
# 1. plistにプラグインディレクトリを直接書き込む
defaults write com.ameba.SwiftBar PluginDirectory "/Users/user/Library/Application Support/SwiftBar/plugins"

# 2. アプリケーションを再起動する
killall SwiftBar
sleep 1
open -a SwiftBar
```

### 教訓
macOS のメニューバーユーティリティを自動導入・設定する場合、単純にアプリを起動するだけでなく、`defaults` や設定ファイル（`.plist`等）を直接パース・修正して初期ダイアログや選択手順をバイパスしてあげることが、シームレスな「一撃導入」には極めて重要である。

## 2. macOS GUIアプリ実行環境における環境変数（PATH）問題

### 事象
ターミナル上の手動実行では正常に `node` を呼び出せていたスクリプトが、SwiftBar などの GUI アプリケーションからバックグラウンド実行された際、外部コマンド（`node` 等）の呼び出しに失敗してフォールバック動作になってしまっていた。

### 原因
macOS の GUI アプリから起動されるプロセスの環境変数（特に `$PATH`）は、ユーザーのシェル（`.zshrc` 等）の設定を引き継がず、最小限のシステムパス（`/usr/bin:/bin` 等）のみになる。そのため、Homebrew 等でインストールした `/opt/homebrew/bin/node` へのパスが通らず、コマンドが実行できなかった。

### 対策
Python スクリプト等の外部プロセス起動ロジックにおいて、直接バイナリ名（`node`）で呼ぶのではなく、以下の絶対パス自動検出ヘルパーを実装して絶対パスで呼び出すようにする。

```python
def get_node_path():
    paths = [
        "/opt/homebrew/bin/node",
        "/usr/local/bin/node",
        "/usr/bin/node"
    ]
    for p in paths:
        if os.path.exists(p):
            return p
    return "node"
```

### 教訓
GUI 拡張機能やメニューバープラグインなどで、外部のランタイム（Node.js, Python, CLIツール等）をサブプロセスとして起動する際は、絶対に環境変数（PATH）を前提とした設計にしてはならない。必ず想定される絶対パスを自動検出するか、フルパスで呼び出す仕組みを徹底する必要がある。

## 3. SPA（React）の動的画面におけるアニメーション・ローディングの待機問題

### 事象
CDP（Chrome DevTools Protocol）経由で設定モーダルを開き、Modelsタブへクリック遷移した直後にDOMを取得すると、ゲージの目盛りセグメントが描画途中（またはローディング中）のため、すべて0%（非点灯）または誤ったデフォルト値としてスクレイピングされてしまう。

### 原因
シングルページアプリケーション（SPA）では、モーダルの展開アニメーションやAPIからのデータフェッチおよび再描画に数百ミリ秒から数秒のラグが存在する。直後の評価（evaluate）はロード前の一時的なDOMや0%の過渡期状態を拾ってしまうため。

### 対策
- モーダルクリックやタブ切り替えなどのトリガー操作ごとに、十分なアニメーション・描画完了ディレイ（設定オープンに `1.0秒`、モデル切り替えに `1.5秒` 等）を明示的に挟む。
- 単純なテキストパースだけでなく、行コンテナのDOM配下にあるセグメント要素の `style` 属性における `width: 100%` を厳密に走査・カウントする多重構造のフォールバック付きロジックを実装する。
- サブプロセスのタイムアウト値（Python側の `timeout`）を十分な長さ（10秒程度）に延長し、SPAの描画遅延による異常終了を防止する。

### 教訓
動的SPAや複雑なReactアプリケーションのUIをバックグラウンド操作（CDP等）でスクレイピングする際は、必ずアニメーションとAPIフェッチに伴う時間差を考慮した適切なシーケンス制御（段階的なディレイ）を徹底しなければならない。また、過渡的な状態を誤検知しないよう、DOMの属性（`style` の幅など）を厳密にスキャンする走査アルゴリズムの二重の防護設計が必要不可欠である。

## 4. SwiftBar / xbar の予約文字（デリミタ）との干渉問題

### 事象
メニューバーのタイトル文字列に ` | `（半角の縦棒）を使用すると、最初の `|` より後ろのモデル項目がすべてカットされ、最初のモデル（`F-Med:🟢`）しか表示されなくなってしまう。

### 原因
SwiftBar および xbar フォーマットにおいて、`|` は「タイトル文字列」と「パラメータ項目（フォントや色など）」を区切るためのシステム予約文字であるため。タイトル内に `|` を含めると、最初の `|` 以降がすべてパラメータオプションとして解釈されてしまい、タイトルから削除されてしまう。

### 対策
メニューバータイトルを結合する際の区切り文字に `|` ではなく、システムと干渉しない安全な記号「`  •  `」（半角スペース2つ＋バレット記号＋半角スペース2つ）や「`  `」（スペース複数）などを使用する。

### 教訓
外部ツールの標準出力インターフェース（特にSwiftBarやxbar等）にテキストを渡す際は、そのパーサー仕様を厳密に把握し、予約デリミタ（`|` など）との意図しない競合を防ぐために、代替の安全な文字（バレット記号等）を選択するかエスケープ処理を必ず徹底すること。

## 5. シンボリックリンク配置スクリプトにおけるカレントディレクトリ解決問題

### 事象
ターミナル上の直起動では正常にクォータ情報（%表示）がリアルタイム同期できていたが、SwiftBar 経由でバックグラウンド実行された際、常にキャッシュ表示のフォールバック動作になってしまっていた。

### 原因
SwiftBar のプラグインディレクトリには、スクリプトへのシンボリックリンクが配置されている。Pythonスクリプト内で `os.path.abspath(__file__)` を使用してカレントディレクトリを解決すると、シンボリックリンクが置かれている場所（`~/Library/Application Support/SwiftBar/plugins/`）が基準となり、隣にあるはずの `get_quota.js` を見つけられず、スクレイピング実行に失敗していた。

### 対策
スクリプトの位置を特定する際は、`os.path.abspath(__file__)` ではなく `os.path.realpath(__file__)` を使用し、シンボリックリンクを自動的に実体パスへ解決してからディレクトリ名を特定する。
`SCRIPT_DIR = os.path.dirname(os.path.realpath(__file__))`

### 教訓
OSの拡張機能やタスクスケジューラなどで、シンボリックリンク経由で実行される可能性のあるスクリプトを開発する際は、絶対に `abspath` を使ってはならない。必ず `realpath` を用いてリンクの実体パスを解決した上で相対ファイル参照を行う設計を徹底すること。

## 6. SwiftBar プラグインメタデータの欠落による [?] 警告プレースホルダーの発生

### 事象
メニューバーのプラグイン表示の左端に、無効な警告プレースホルダー `[?]` マークが表示されていた。

### 原因
SwiftBarの公式仕様に準拠したメタデータがスクリプト内に記述されていなかったため、アプリ側でプラグインのアイコンや情報が欠損していると判定され、警告プレースホルダーが出力されていた。

### 対策
スクリプトの先頭（シバン行の直後）に、`<swiftbar.title>` や `<swiftbar.icon>` などの公式メタデータタグを含むコメントブロックを追加する。

### 教訓
外部の統合ランチャー（SwiftBar, xbar, Alfred, Raycast等）向けに拡張機能を開発する際は、単に標準出力をフォーマットするだけでなく、そのプラットフォームが要求するメタデータ定義（ヘッダー記述など）を最初から完璧に満たしておくこと。これにより、プレースホルダーや読み込み警告を未然に防ぐことができる。

## 7. 自動GUI操作スクリプトの実行頻度とユーザー体験の競合（ウィンドウポップアップ干渉）

### 事象
メニューバーの更新周期（30秒）に合わせて毎回ブラウザの設定モーダルが目の前で自動開閉してしまい、ユーザーのタイピング作業を著しく妨げていた。

### 原因
正確なモデルクォータをDOMから取得するために、30秒に1回CDP（Runtime.evaluate）経由で設定モーダル（Settings > Models）の自動開閉処理を同期的に走らせていたため。

### 対策
- **スマートキャッシュ制御（Bypass）の導入**:
  - `quota_cache.json` に最新フェッチ日時を記録し、前回のフェッチから **15分** 経過していない場合はCDP操作を完全にスキップさせ、キャッシュから **0.04秒** で超高速出力する。
  - 平常時の自動更新では目の前で設定ウィンドウは一切開かないように制御。
- **強制更新トリガーの統合**:
  - メニュー内の「🔄 再読み込み」をクリックした時に限り、SwiftBarの `bash` オプション経由で `--force` 引数を与えてスクリプトを再起動させ、その場でのみ強制的にリアルタイム同期を実行する。

### 教訓
CDPなどのブラウザ自動操作は非常に強力だが、ユーザーがアクティブに操作している対話ブラウザと同じコンテキストで動作する場合、頻繁な自動操作は深刻なユーザー体験の干渉（ウィンドウのポップアップ等）を引き起こす。
このようなケースでは、**「平常時は超高速なキャッシュ表示（無干渉動作）」に徹し、重い・干渉する自動操作は「低頻度（15分や30分に1回）のバックグラウンド実行」や「ユーザーが明示的にRefreshをクリックした時」にのみ限定するスマートキャッシュ設計**を徹底しなければならない。


## 8. LSPのConnect API直接フェッチへの刷新による画面非干渉化

### 事象
ブラウザ自動操作（CDP経由のモーダル開閉）では、キャッシュ時間を15分に引き延ばしても、初回起動時や強制同期時にどうしても一瞬ウィンドウが開閉してしまい、プロ仕様のツールとして美しさに欠けていた。

### 原因
設定画面のDOMからしかクォータ残量を取得できないと思い込んでいたため、ブラウザ操作（設定ボタンクリック -> Modelsタブ遷移 -> DOM解析 -> モーダルクローズ）を余儀なくされていた。

### 対策
- 常駐している `language_server` プロセスを `ps aux` から特定し、引数の `--csrf_token` からCSRFトークンを抽出。
- `lsof -i` から該当PIDがLISTENしているローカルAPIポートを自動特定。
- `/exa.language_server_pb.LanguageServerService/GetUserStatus` に対して、HTTP POSTで直接Connect APIリクエストを送信。
- 画面開閉は1ミリも発生させず、完全に無音・バックグラウンドで100%正確なモデルクォータを取得することに成功。API呼出が非常に軽量なため、キャッシュ時間（以前は15分）を `0秒` に設定し、毎回の更新で完全リアルタイム同期が可能に。

### 教訓
画面上のDOMをスクレイピングする原始的なアプローチをとる前に、対象プロダクトのバックグラウンドプロセス（Language Serverなど）が提供するローカルAPIやソケットが存在しないかを深く調査し、リバースエンジニアリングや直接通信によるハックを検討すること。これにより、ユーザー体験（UX）を損なわない完全にクリーンでプロ仕様のツールを構築できる。

## 9. SwiftBarプラグインディレクトリ内の不要なデータ・キャッシュファイル誤認識による [?] エラー問題

### 事象
メニューバーのクォータ情報は正常に表示されているにもかかわらず、その右隣にクエスチョンマーク付きの警告アイコン `[?]`（プラグインエラー）が表示されてしまっていた。

### 原因
キャッシュファイル `quota_cache.json` が、スクリプトと同階層（`SCRIPT_DIR` 即ち SwiftBarの `PluginDirectory`）に作成されていた。
SwiftBarはプラグインディレクトリにあるすべてのファイルをプラグイン（スクリプト）としてロード・実行しようとするため、JSONファイルである `quota_cache.json` を実行してエラー（例外）を吐き出し、それがメニューバー上の `[?]` 警告エラー表示を引き起こしていた。

### 対策
- キャッシュファイルの保存パスを、プラグインディレクトリの外（例：`~/.gemini/antigravity/quota_cache.json`）に変更する。
  `QUOTA_CACHE_FILE = os.path.expanduser("~/.gemini/antigravity/quota_cache.json")`
- プラグインディレクトリ内に生成されてしまっていた不要な `quota_cache.json` をコマンド `rm` で完全に削除する。
- SwiftBarを再起動し、誤認識プラグインを排除する。

### 教訓
SwiftBarやxbarなどのプラグインディレクトリは、**実行可能なスクリプトファイルのみを配置する純粋なディレクトリ**として保つ必要がある。スクリプトが作成するデータ、キャッシュ、一時ファイル、アセットなどの不要なファイルは、絶対に同ディレクトリ内に書き出してはならない。必ずホームディレクトリ下のアプリデータ領域や一時ディレクトリ（`/tmp` 等）を指定し、プラグインディレクトリのクリーンさを維持すること。

## 10. 多段階（グラデーション）カラーと絵文字マッピングによる視認性と高級感の向上

### 事象
各モデルのクォータ残量表示について、単なる3段階（🟢, 🟡, 🔴）の分類から、ユーザー指定による6段階の微細なグラデーション（🟣 100% / 🔵 80% / 🟢 60% / 🟡 40% / 🟠 20% / 🔴 0%）へ表現形式を刷新。

### 対策
- `get_quota_emoji` 関数にて、100% (🟣) およびそれ以下の80%, 60%, 40%, 20%, 0%の各閾値を詳細に判定し、それぞれに適した絵文字をマッピング。
- 同時に `get_quota_color` 関数を導入し、SwiftBarのドロップダウン詳細行のフォントカラー（Hex）を上記の絵文字カラーと同調する美しい色彩（紫、青、緑、黄、橙、赤）に統一した。
- 当初は100%（満タン）状態の数値をあえて省略し `🟣` 単体としていたが、すべてのモデルが同じ表記フォーマット（例：`Sonnet:🟣100%`）で統一されている方が情報の一貫性と視認性が高く、ユーザーにとって直感的であるため、100%の時も `🟣100%` とパーセンテージを明記する仕様へアップデートした。

### 教訓
- メニューバーのように表示領域が限られたUIであっても、**「すべての項目で同一の表示ルール（絵文字＋数値％）を徹底すること（一貫性）」**が、ユーザーの視覚的な混乱を防ぎ、一目で全ての状態を美しく比較するために極めて重要である。
- また、絵文字とテキスト色を同期させることで、「高級感・プレミアム感」が劇的に向上し、プロダクトとしての品質がワンランク上がる。
- テストコードの構築においては、具体的な絵文字や色などの「デザイン表現」に依存せず、部分文字列の存在チェック（`assertIn` など）を活用した柔軟なアサーション設計をしておくことで、表示カスタマイズ時にテストが壊れず安全にアップデートできる。

## 11. SwiftBarプラグインにおけるクリック一つの多言語切り替え（i18n）アーキテクチャ

### 事象
メニューバーの表示文言がハードコーディングされた日本語のみであったため、英語圏ユーザーには使いづらく、グローバル展開に不向きだった。

### 対策
- **`MESSAGES` 辞書パターン**: スクリプト先頭にキー付きの多言語メッセージ辞書 `MESSAGES = {"en": {...}, "ja": {...}}` を定義し、すべてのUI文字列を辞書からの参照に切り替えた。
- **`--set-lang` コマンドライン引数**: ドロップダウンの言語選択UIクリック時に `bash` パラメータ経由で `--set-lang en` または `--set-lang ja` を実行し、`quota_cache.json` にユーザー設定を永続化する設計とした。
- **チェックマーク `[✓]` による現在言語の可視化**: 現在選択されている言語にのみ `[✓]` を表示し、直感的に現在のモードを伝えるUI設計にした。
- **`format_reset_time(iso_str, lang)` への `lang` 引数の追加**: リセット時間表示（`reset` vs `リセット`）も含めて全テキストを言語に応じて動的に出し分ける完全な対応を実現。

### 教訓
- SwiftBarのようなメニューバーツールで多言語対応を行う際は、**ドロップダウンメニュー内に言語選択UIを組み込み、クリック一つでキャッシュファイルに書き込む `--set-lang` パターン**が、設定ファイルの手編集を不要にするシームレスな手法として極めて有効。
- ハードコーディングされた文字列を後から多言語化する際は、「辞書 + `msg = MESSAGES[lang]` パターン」で**すべての文字列参照を一箇所に集約**し、今後の言語追加（例：中国語、韓国語）にも1辞書ブロックの追加だけで対応可能な拡張性を確保すること。
- テストコードにおいては、**言語ごとに個別のテストケースを用意**し、英語テスト・日本語テストをそれぞれ独立したアサーションで検証することで、意図しない表記混在バグを完全に防止できる。

## 12. gRPC-Web (proto3) JSONシリアライズにおけるクォータ0%（デフォルト値）フィールド欠落バグと、lsofのPID指定によるディレイ解消

### 事象
- クォータが0%になったモデル（Claude Sonnet/Opusなど）が、タスクバー上（SwiftBar）で `0%` ではなく `100% (🟣)` と表示されるバグが発生。
- IDE側のModels画面とステータスバー表示の間にディレイ（タイムラグ）があり、0%への反映が遅れる問題があった。

### 原因
1. **gRPC-Web / proto3 のシリアライズ仕様**: クォータが消費されて0%になった際、APIレスポンス（Connectプロトコル）内の `remainingFraction` フィールドの値が `0`（デフォルト値）になります。proto3の仕様では、数値型のデフォルト値（`0` または `0.0`）はJSONシリアライズ時に**フィールドごと省略**されます。従来のスクリプトでは「フィールドが無い場合はクォータに問題がない（100%）」と解釈していたため、0%になったモデルが100%と誤判定されていました。
2. **lsofのシステム全体スキャンによる遅延**: LSPプロセスのポートを特定する際、`lsof -i -P -n` を実行していましたが、システム全体の全ネットワークソケットをスキャンするため実行時間が数秒以上かかり、SwiftBarのタイムアウト制限（10秒）を引き起こしていました。このため、キャッシュが古い値（20%など）のままフォールバックされ続け、表示に大きなディレイが発生していました。

### 対策
- **欠落フィールドの0%解釈**: `quotaInfo` オブジェクトが存在するにもかかわらず `remainingFraction` が省略されている場合、それは「クォータ残量 0 (0%)」とみなすように `get_quota.js` の判定ロジックを修正。
- **lsofの特定PID絞り込み**: `lsof -a -p ${pid} -i -P -n | grep LISTEN` のように `-p` オプションで対象のLSPプロセスIDを直接指定し、ソケット探索を一瞬（10倍以上高速）で完了するように改善。

### 教訓
- gRPC や Protocol Buffers (proto3) を用いるAPIをリバースエンジニアリングする際、**「数値が 0 (デフォルト値) の場合はフィールド自体がJSONから消滅する」**というシリアライズ仕様を念頭に置いて判定ロジック（`undefined`の扱い）を実装しなければならない。
- `lsof` などのシステムリソーススキャンコマンドをスクリプトで定期実行（ポーリングなど）する際は、スキャン範囲をグローバルにせず、必ず **特定PIDやポートに絞るオプションを指定してスキャンのオーバーヘッドを極小化する** ことが、タイムアウトや動作のディレイを防ぐために極めて重要である。

## 13. 本番稼働用サブフォルダ（app）のクリーン化による開発環境の完全分離

### 事象
- プロジェクト成果物をデスクトップの `AntigravityQuarterViewer` に移行するにあたり、SwiftBarプラグインとして本番稼働する実行環境（`app/` サブフォルダ）の中に、テストコード、パッケージ定義、タスク管理ドキュメントなどの開発用ファイルを混入させないよう厳密に分離・整理した。

### 原因・背景
- 開発時および保守段階では、テストコードやタスク管理のドキュメントが同階層に存在すると見通しが良くなりますが、本番稼働環境（特にプラグインディレクトリや自動ロード環境）にこれらが混入すると、意図しないファイル誤認識（前述の `quota_cache.json` などの誤ロード問題など）や環境の肥大化、セキュリティリスクを招く原因になります。
- メイン実行ファイルの `get_quota.js` はNode.jsの標準ビルトインモジュールのみで実装されており、`node_modules` や `package.json` の本番配置は一切不要であるため、完全な最小構成に整理が可能でした。

### 対策
- 新しいデスクトップ上の配置において、直下ルートに開発用ファイル（テストコード、タスク、package.json、その他デバッグ用スクリプト群）をすべて残しつつ、実行に必要な `antigravity_status.py` と `get_quota.js` の **2ファイルのみ** を本番稼働用のサブフォルダ `app/` にクリーン配置。
- SwiftBarのプラグイン登録先（シンボリックリンク）を、このクリーンな `app/antigravity_status.py` に指定して稼働させました。

### 教訓
- アプリケーションやツールをユーザー環境にデプロイ・配備する際は、**「本番動作に本当に必要な最小限のファイル」と「開発時にのみ使用するテスト・管理用ファイル」を完全に分離したフォルダ構成を設計する** ことが必須である。
- 特に、外部パッケージ（`node_modules`）への依存がないなど、実行環境を完全に独立（クリーン）に保てる設計になっている場合は、実行用サブフォルダに余分な開発アセットや構成ファイルを一切立ち入らせないことで、バグの未然防止、保守性の向上、そしてプロフェッショナルな配布パッケージとしての美しさを保つことができる。


## 14. SwiftBarデフォルトシステムメニュー（About / Run in Terminal / Disable Plugin）の完全非表示化と、ドロップダウン内への独自インタラクティブAboutセクションの実装

### 事象
- SwiftBar標準のAboutダイアログは表示レイアウトの自由度が著しく制限されており、表示された note のWebsite URLリンクが活性化（青文字化・クリック可能）されないというUX上の課題が発生した。
- また、標準の「Run in Terminal...」や「Disable Plugin」などの開発用デフォルトメニューが常に表示されていることで、メニュー全体の高級感と見通しが損なわれていた。

### 原因
- SwiftBarがプラグインごとに自動生成する標準About画面は、アプリ内部のSwiftコードでデザインが固定されているため、プラグインのメタデータタグのみではURLをハイパーリンク（クリック可能な青文字）化することが不可能な仕様となっていた。

### 対策
標準機能の限界を美しく解決するため、以下の「ハイブリッド隠蔽＆独自メニュー設計」に刷新した：
1. **標準デフォルトメニューの完全排除**:
   メタデータヘッダーに `swiftbar` プレフィックスを統一し、以下の隠蔽タグを設定して、標準メニュー3つ（About, Run in Terminal..., Disable Plugin）を完璧に消去した。
   ```bash
   #<swiftbar.title>Antigravity Quota Monitor</swiftbar.title>
   #<swiftbar.version>1.2</swiftbar.version>
   #<swiftbar.author>Madoka</swiftbar.author>
   #<swiftbar.desc>Antigravity Quota & Credit Monitor</swiftbar.desc>
   #<swiftbar.icon>👾</swiftbar.icon>
   #<swiftbar.hideAbout>true</swiftbar.hideAbout>
   #<swiftbar.hideRunInTerminal>true</swiftbar.hideRunInTerminal>
   #<swiftbar.hideDisablePlugin>true</swiftbar.hideDisablePlugin>
   ```
2. **ドロップダウン内への独自About実装**:
   プラグインのドロップダウンメニューの最下部に、多言語（日本語・英語）に完全対応した `ℹ️ About` セクションを実装。
   SwiftBarの標準出力仕様の `href` オプション（例：`href=https://note.com/us_kabu_journal`）を利用することで、Websiteリンク部分が「完全に活性化（美しいOS標準ブルーでハイパーリンク化）」され、ワンクリックでnoteにジャンプする完璧なUXを実現した。
   ```bash
   ℹ️ About
     ・Antigravity Quota Monitor: v1.2
     ・Website: https://note.com/us_kabu_journal  (活性リンク)
     * Copyright © 2026 US stock journal. All rights reserved.
   ```

### 教訓
外部統合環境（SwiftBar/xbar等）の「標準搭載機能」にUI制限や表示バグがある場合、それらを無理に設定変更しようとするのではなく、**「標準のデフォルトメニューを完全に隠蔽（hide設定）し、自前でドロップダウン内の独自セクションとして完璧に再実装する」**という設計思想への転換が極めて有効である。特に、SwiftBarが提供する `href` パラメータ等の強力なシェル拡張機能を活用することで、ネイティブダイアログよりもはるかに高機能で視覚的にも優れた「本物」のUXを100%自らのコントロール下に構築できる。

## 15. VS Code拡張機能におけるREADME画像のマークダウン記法と.vscodeignoreの関係

### 事象
VS Code Marketplace や Open VSX に拡張機能を公開した際、ローカル環境では正常に表示されていた README.md 内のバナー画像（`banner.png` 等）が表示されなくなってしまう（またはリンク切れになる）問題が発生した。

### 原因
1. **`.vscodeignore` による画像ファイルの除外**: 当初、拡張機能の軽量化のために `*.png` を除外リストに入れていた。特定ファイル（`!banner.png`等）でホワイトリスト化していたものの、確実なバンドルのためには画像管理の明確なルール設定が必要。
2. **VSCE の画像パス解釈仕様（HTML vs Markdown）**: 元の README.md では `<img src="banner.png">` という HTML タグを使用していた。VS Code Marketplaceの仕様において、HTMLの `img` タグ内の相対パスは正しく解釈されず、リポジトリURLへ自動置換されない場合が多い。

### 対策
- README内の画像挿入には、HTMLタグ（`<img src="...">`）ではなく、標準の **Markdown構文（`![Alt Text](banner.png)`）を使用** する。これにより、`vsce` パッケージャーが自動的に `package.json` の `repository` URLを参照し、適切な画像URL（GitHub raw等）へ安全に解決・置換してくれる。
- `.vscodeignore` で不必要にすべての `*.png` を除外しないか、パッケージング時に必要な画像が正しく `.vsix` 内に梱包されていることを確認する。

### 教訓
VS Code拡張機能を開発し公開する際は、**「README.md 内の画像は絶対にHTML構文で記述せず、標準Markdown構文を利用する」** ことを徹底しなければならない。これにより、プラットフォーム（VS Code Marketplace, Open VSX）間の仕様差を吸収し、確実に画像が表示される美しいランディングページを構築することができる。

## 16. リポジトリの公開コンテキストにおける言語優先ルールとユーザー指示の即時適用

### 事象
グローバル向けの公開リポジトリ（Open VSXやGitHubなど）で配布するVS Code拡張機能の `README.md` において、「生成する成果物（READMEなど）も日本語にする」という一般的なプロジェクト優先ルールに従って日本語化を適用したところ、作者・コミュニティ全体のグローバルな可読性を維持したいというユーザー様から「READMEは絶対に英語で！」との強い緊急修正指示を受けた。

### 原因
「開発上のAI行動ルール（日本語化）」を機械的にプロダクトの公開用ドキュメント（特にグローバル向けパッケージの README.md）にそのまま適用してしまったため。プロジェクトの配布コンテキストや、リポジトリが世界向け（英語ベース）に構築されている点に対する配慮が不足していた。

### 対策
- ユーザー様からの指示（「READMEは絶対に英語で！」）を最優先として即座に受け入れ、README.md 全体を元の洗練された英語表記へ完全に復元。
- 今回追加したアライメント縦整列、リセット日時の動的表示、透過シアンのプレミアムアイコン化、アセット画像のローカル梱包といった V1.3.0 〜 V1.3.1 の Changelog もすべて完全な英語（Changelogセクション）で追記した。
- バージョンを `1.3.1` へバンプし、即座にビルドとパブリッシュを完了させて「速攻適用」を実証した。

### 教訓
成果物の言語ルールは、**「プロジェクト全体の配布・公開コンテキスト（GitHubやグローバルマーケットプレイス等）」および「ユーザー様からの直接指示」を最優先**しなければならない。特に公開されるプロダクトドキュメントは、単なるAIのルール適合ではなく、グローバルユーザーへのアピール度やブランド価値を最重視した言語選択を行い、ユーザー指示があった場合はミリ秒単位での速攻修正と適用を徹底すること。

## 17. VS CodeマニフェストにおけるOS制限仕様の差異と、ランタイム警告によるUX設計の最適化

### 事象
Windows/Linuxユーザーのインストール混乱を防ぐため、`package.json` に `"os": ["darwin"]` を記述してパブリッシュしようとした際、ユーザー様からの鋭い「darwinで本当にあってる？」というダブルチェックを契機に技術仕様を再調査。結果として、VS Codeの `package.json` マニフェストは、インストール段階でのOS制限機能を標準サポートしておらず、このフィールドは単に無視される（他OSでもインストールできてしまう）という仕様上の差異を特定した。

### 原因
npmパッケージの一般的な `package.json` の仕様（`os` 制限）と、VS Code Extension のマニフェストの独自仕様の差異を混同し、仕様のダブルチェックを行わずに設定を適用しようとしていたため。

### 対策
- 機能しない `"os": ["darwin"]` の記述を `package.json` から直ちに削除し、構成をクリーン化。
- 代わりに、拡張機能の起動ロジックである `extension.js` のアクティベーション処理の冒頭に、ランタイム判定コード `if (os.platform() !== 'darwin')` を挿入。
- 非macOS環境で起動された場合に、VS Codeのネイティブ警告 `vscode.window.showWarningMessage('Antigravity Quota Monitor (AQM) is currently only supported on macOS (requires SwiftBar).')` をトーストで綺麗に出しつつ処理を `return` させる安全設計を実装。
- バージョンを `1.3.4` にバンプし、再ビルド、GitHubへのプッシュ、Open VSXへのパブリッシュを即時に実行完了した。

### 教訓
マーケットプレイスにデプロイするメタデータや動作制限機能は、一般的なNode.jsの仕様とマーケットプレイス独自の仕様（VS Code Extension仕様）の差異を常に意識し、二重検証を行うこと。また、OS依存のツールを配布する際は、インストール制限だけでなく、**「サポート外OSでアクティベートされた際、無言で停止するのではなく、右下の警告トースト等でユーザーに優雅かつ親切に動作要件を伝えるランタイム警告設計（UXの保護）」**を徹底すること。

## 18. APIレスポンスデータの仕様誤認に伴う「リセット時間の常時返却」の罠と、シンプル設計への回帰

### 事象
クォータが正常（満タン / 100%）に機能している場合であっても、メニューバー上の表示がすべて `0% 🔴` に固定されてしまう壊滅的な表示バグが発生した。

### 原因
LSPの Connect API 応答内の `resetTime`（回復時間）フィールドは、**「現在クォータ制限がかかっているかどうかに関係なく、常に次回の定期リセット日時（未来の時刻）を返却し続ける仕様」** であった。
前回の「LSP起動時の同期ラグ（不整合）」を無理に防ごうとして追加した `if (resetTime > now) { remainingFraction = 0; }` という補正ロジックが災いし、満タンで正常なモデルの `resetTime` まで未来を指していることから、すべてのモデルを一律で 0% 🔴 に強制書き換えしてしまった。

### 対策
- 間違った日時補正ロジックを即刻「完全撤去」し、元の「`remainingFraction` が数値型ならそのパーセンテージを採用し、省略時は 0% とする」最も安定したシンプルなロジックへと安全に完全復元。
- 手動テストおよび Python 連携テストを再実行し、正常なモデルが `100%` や `80%` と正しくリアルタイムに描画されることを実証・確認した。
- バージョンは `1.3.7` のまま、手元検証用の `.vsix` パッケージを再ビルドした。

### 教訓
- API から取得した日時情報（`resetTime` など）をトリガーにしてステータスを強制補正する際は、**「正常な時でもその日時は未来を指すのか」という API の静的・動的な仕様（状態遷移）を徹底的に検証・確認** しなければならない。
- 一時的な起動ラグ（一瞬100%に見えて数秒で0%に落ちる現象）のような過渡的な不整合は、時間が解決する軽微なものである。そのため、下手にフロント側で先読み補正をかけて複雑なロジックを組むより、**「元の最もシンプルで頑丈なロジックに任せる（複雑な過剰エンジニアリングを避ける）」** ことが、長期的な安定性と品質を守るための鉄則である。

## 19. アクティブなLSPログの動的切り替えと、会話メタデータからの超高速な承認状態の検出設計

### 事象
- エージェントのステータス（思考中、承認待ちなど）をリアルタイムに検知するシステムバー（SwiftBar）表示の開発において、従来の `daemon/ls_*.log` は一部の起動環境で書き込みが停止しており、実際には `~/Library/Logs/Antigravity/language_server.log` にログが出力されていた。
- また、ログのデバッグレベルや出力タイミングによっては、ユーザー承認待ち（問合せや承認ダイアログ）のトリガーログを正確にフックする難易度が高かった。

### 解決策
1. **ログファイルの動的優先切り替えロジック**: `language_server.log` の有無を最優先でチェックし、フォールバックとして従来の daemon ログや他の IDE ログディレクトリも順次スキャンして最も新しいファイルを動的に特定する設計（`get_latest_log_file`）を導入し、完全な堅牢性を確保した。
2. **会話フォルダのメタデータ連携による承認状態の超高速フック**: `~/.gemini/antigravity/brain/` にある最新の会話フォルダの `*.metadata.json` をパースし、いずれかのファイルに `"requestFeedback": true`（＝ユーザーの入力・承認待ち状態）が立っているかを直接スキャンする判定エンジンを設計した。これにより、ログの記述規則に一切依存しない、100%正確で超高速なフック（誤検知ゼロ）を実現した。

### 教訓
- プロセスの状態を監視する際、単一のログファイルパスのみを固定の監視対象にすると、実行環境やバージョンアップによるパスのズレで壊滅的なバグになりやすい。優先パスを定義しつつ、フォールバックの検索範囲を動的に広げる「自動切り替え設計」にすること。
- また、ログのテキストパースといった不安定で曖昧な手法だけでなく、**「システムやフレームワークがディスクに直接書き出すセッション状態（メタデータやタスク状態のJSONなど）」を並行スキャンすること**が、状態検知の確実性とパフォーマンスを最大化するための極めて強力なアプローチである。

## 20. メニューバーにおける「常時クォータ情報維持 ＆ プレフィックス状態切り替え」UXと、未来時刻ズレによるフック固着バグの回避

### 事象
- エージェントのステータス（思考中や承認待ち）をシステムバーに表示する際、当初はタイトル文字列全体を「思考中...」に切り替える方式にしていたが、ユーザーから「思考中でも現在のモデル別残数値（クォータ）は常に並べて見ていたい」という強力なUX上のフィードバックが発生した。
- また、エージェントが動作を終えて完全にアイドル状態になった後も、システムバーが `🧠`（思考中）のまま元に戻らない（固着する）壊滅的なバグが発生した。

### 解決策
1. **左端アイコンのみの動的プレフィックス統合UI**: 表示情報量を1ミリも犠牲にしないため、通常時のクォータ横並び文字列を完全に残したまま、**一番左側のアイコンのみ**を状態に応じて動的にスワップ（待機中: `👾`, 思考中: `🧠`, 承認待ち: `⚠️`, 停止: `⚪️`）する統合設計へと刷新した。承認待ちの際は、テキスト色オプション（`| color=#ff3b30`）を結合してテキスト全体を赤文字で点灯させるハイライトも統合した。
2. **タイムスタンプのレンジ制限 (`0 <= diff <= 30`)**: システム現在時刻（`now`）とログからパースしたタイムスタンプ（`log_dt`）にミリ秒単位のズレがあり、ログのほうがわずかに未来を指していた場合、差分秒がマイナス（負の値）になってしまい、従来の `diff <= 30` 判定では未来時間もすべて「30秒以内」と判定されて「思考中が永久に解除されない」バグになっていた。これを防ぐため、下限値を設けた `0 <= diff <= 30` という厳密なレンジ制限に修正した。
3. **ログ更新停止（`mtime`）との連動前提条件**: ログファイル自体の最終更新日時（`mtime`）が現在時刻から15秒以上前（＝LSPからのログ書き込みがストップしている状態）であれば、自動的に `is_thinking = False` と判定する防護設計を組み込んだ。

### 教訓
- メニューバーのように限られた領域でステータスを可視化する際、「画面の完全切り替え」は重要な監視データ（クォータ残量）を一時的に隠蔽してしまうため、UXの観点からは悪手になりやすい。**「ベースとなる監視テキストは常時維持したまま、左端のステータスアイコンのみを動的に変化させる統合アプローチ」**が、高い情報量とリアルタイム状態把握を両立する最もプロフェッショナルでプレミアムな設計である。
- 時刻比較ロジックを実装する際は、システムクロックとデータソースのタイムスタンプに生じうる「未来方向のわずかなズレ（ミリ秒の誤差）」を常に予期し、負の値の発生を防ぐために `0 <= diff` という下限しきい値や、ログファイルの `mtime`（物理的な更新停止）を組み合わせた**多重防護設計**を徹底しなければならない。

## 21. 非干渉な太いデリミタ `❘` によるUI分割の明示と、秒数ハックに基づくバグゼロな動的交互アニメーション設計

### 事象
メニューバー上の表示が「絵文字だけ」だと状態が分かりにくく、さらにステータスとクォータ残量の区切りが視覚的に混ざって見にくかった。また、エージェントの思考中に `🧠` と `🌀` などの回転・点滅アニメーションやドット循環表示を、状態保存ファイルなどを使わずに安全に実装する必要があった。

### 原因・背景
- SwiftBar の標準タイトルでは、システム予約デリミタ `|` を使用するとそれ以降のクォータ文字列が切り落とされるバグ（仕様制限）が発生する。
- 外部プロセスが完全にステートレスに動くため、ファイルI/Oを用いてアニメーションのフレーム（状態）を管理すると、ファイルの競合やクリーンアップ漏れ、アニメーション停止などの深刻なバグが発生しやすかった。

### 対策
1. **非干渉な太いデリミタ `❘` (U+2758) によるUI分離**: SwiftBarのデリミタ判定を完全に回避する `❘` を採用し、左側に「絵文字 ＋ 状態テキスト (Idle / Thinking / Pending)」、右側に「モデル別クォータ残量」を配置することで、100%安全かつ極めて見やすく分離されたプレミアムUIを実現。
2. **システム時刻の秒数ハックによるステートレスアニメーション**: 現在時刻の「秒」から動的に循環サイクルを算出するロジック `cycle = (now.second // 5) % 4` を開発。これにより、5秒ごとに `0 ➔ 1 ➔ 2 ➔ 3 ➔ 0` とインデックスが切り替わり、
   - **思考中 (`thinking`)**解決策: 欠落していた `import subprocess` を追加した。また、`ps` コマンドでプロセスを抽出する際、ターミナル幅による切り捨てを防ぐために `/bin/ps -eo pid,command` のような確実な絶対パス・フォーマット指定を使用するようにロジックを根本的に堅牢化した。

### 8. SwiftBarにおけるパラメータパースの脆弱性とインデントの消失
**問題**: `| font=sans-serif size=12 bold=true` のように、SwiftBarでサポートされていないプロパティ（`bold=true`など）をパラメータ文字列に含めると、SwiftBarはその行のパラメータ全体（`color`など）をパースエラーとして**無音で完全に無視**してしまう。また、メニュー行の先頭にある通常の半角スペースはSwiftBarによって自動的にトリム（削除）されるため、意図した字下げ（インデント）が失われ、UIのレイアウトが崩壊する。
**解決策**: サポート外のプロパティ（`bold=true`等）は絶対に使用しないよう修正。また、字下げを確実に行うため、通常の半角スペースの代わりに**Non-Breaking Space（`\u00A0`）** を用いることで、トリムを回避し美しいインデントを維持した。

### 9. urllibにおけるProxy汚染とAPIフォールバック（ハードコード値）のサイレントバグ
**問題**: SwiftBar経由で実行された際、Pythonの `urllib.request.urlopen` が意図せずシステムのプロキシ環境変数を拾ってしまい、127.0.0.1へのAPIリクエストが失敗し続ける現象が発生。さらにAPI取得に失敗した際に、開発時にテスト用として `DEFAULT_QUOTAS` にハードコードした `P-Low: 40` が恒久的に表示され続け、「特定のモデルだけ常に40%で連動しない」というユーザーを混乱させる重大なバグを引き起こした。また複数の言語サーバー（IDEやHub）が混在していると、間違ったHTTPSポートを引き当ててエラーになるケースもあった。
**解決策**: 
1. `urllib.request.ProxyHandler({})` を明示的に設定し、プロキシを無効化して確実にローカル通信を通すようにした。
2. 存在する全言語サーバーの全ポートを総当たりでチェックし、成功するまで試行する堅牢なフェッチロジックに改修した。
3. `DEFAULT_QUOTAS` の仮のハードコード値をすべて `100` にリセットし、誤解を招く数値を徹底排除した。
4. エラーの握り潰し（`pass`）をやめ、取得失敗時は `/tmp/agq_error.log` にログを残すようトレーサビリティを確保した。

### 9. urllib.request における HTTPS/Proxy 干渉と複数言語サーバー問題
**問題**: ユーザー環境において `fetch_quota_from_api` が常に失敗し、ダミーの初期キャッシュ値（`P-Low: 40%`等）が表示され続けるバグが発生した。原因は3つ。
1. `urllib` がシステムプロキシ環境変数を自動参照し、127.0.0.1へのリクエストが失敗していた。
2. Antigravity の `language_server` は IDE用と Hub用の複数プロセスが存在し、それぞれ異なるポート（HTTPS/HTTP）を開いている。スクリプトがIDE用のHTTPSポートにHTTPリクエストを送信すると `400 Bad Request` で失敗していた。
3. 初期ダミー値としてテスト用の `40%` がハードコードされていたため、ユーザーから「1つだけ40%で連動していない」と誤解を招いた。
**解決策**:
- `urllib.request.ProxyHandler({})` を設定し、プロキシを無効化した。
- `find_lsp_info` を完全に書き換え、**すべての** `language_server` プロセスをリストアップし、それぞれのプロセスがリッスンしている**全ポート**を抽出するようにした。そして成功するまで全ポートに対してフェッチを試行する堅牢なループへ変更。
- ハードコードされていたダミー値（40%）をすべて 100% に修正し、万が一フェッチに失敗しても不自然に見えないようにした。
   - **承認待ち (`pending`)**: `⚠️ 承認待ち` ➔ `💬 入力要求` (多言語対応) を5秒ごとに赤点滅。
   - **待機中 (`idle`)**: `👾` ➔ `✨` が5秒ごとに煌めくように交互切り替え。
   これらをファイル保存や永続化処理を一切使わずに、完全にバグゼロで滑らかに動作させることに成功した。
3. **余計なインデント記号の完全排除**: ドロップダウン内の `▸` などの余計な記号を完全に廃止し、クリーンな4スペースインデント（`    `）に統一。

### 教訓
- メニューバーにおける表示分割には、プラットフォームの予約デリミタ（`|` など）との競合を避けつつ、見た目の美しさと論理的分割を高レベルで満たすUTF-8非予約系記号（U+2758 `❘` や U+2016 `‖` など）を選択するのが鉄則である。
- 定期実行型のウィジェットやプラグインで「アニメーション」を実現する際は、状態をファイル等に保存して永続化するアプローチ（壊れやすい）を避け、**「システムクロック（現在時刻 of second）から周期サイクルを算出するステートレス設計」**を採用することで、バグを完全に排除し、極めて軽量かつ安定したアニメーションを実装することができる。

## 22. Streamable（常駐ストリーミング）プラグインにおける「ファイル名への更新間隔（時間指定）必須」仕様と自動クリーンアップの罠

### 事象
AQM を Streamable（常駐ストリーミング）プラグインへ移行し、VS Code 拡張機能パッケージ（V1.5.0）をインストールして SwiftBar を再起動した際、古い `antigravity_status.1s.py` などは消え去ったが、メニューバー上の表示が完全に消えて出なくなってしまった。

### 原因
1. **SwiftBar のファイルスキャン仕様（時間指定必須）**: SwiftBar（および xbar）は、プラグインディレクトリ内のファイルをスキャンする際、**「ファイル名に更新間隔の指定（例：`.5s` や `.30s`）が含まれていないファイル（例：`antigravity_status.py`）はプラグインとして認識せず、完全にロード対象外として無視する」**という内部仕様を持っていた。
   - ストリーミング（`streamable`）モードにする場合であっても、ファイル名自体には `{name}.{time}.{ext}`（例：`antigravity_status.5s.py`）という時間指定が必要である。
   - プラグイン内部に `# <swiftbar.type>streamable</swiftbar.type>` が記述されていれば、SwiftBar は起動後に「常駐モード」を認識してファイル名の更新間隔を無視し、無限ループ内のストリームプッシュ（`~~~`）のみに同期するようになる。
2. **手動配置ファイル名の残留競合**: ユーザー環境に手動で作成された `antigravity_status.1s.py` が残っていたため、中身が無限ループであるのに対し、SwiftBar が1秒ごとに重複起動しようとしてハングアップを引き起こしていた。

### 対策
- **`extension.js` でのクリーンアップ対象の最適化**:
  `filesToCleanup` リストに、時間指定のない無効な `'antigravity_status.py'` や、古い `'antigravity_status.1s.py'` などを追加して自動的に綺麗に消去する。
- **時間指定付きの正しいファイル名での展開**:
  コピー先のファイル名を `antigravity_status.5s.py`（または `.10s.py` など）に設定する。ストリームが万が一クラッシュした際のフォールバック周期としても `5s` が最も安定している。
- **プロセスの完全一掃と再起動**:
  古い重複プロセスを `pkill -f antigravity_status` でメモリから一掃し、`killall SwiftBar` を経て SwiftBar を再起動する。

### 教訓
メニューバー統合ユーティリティにおいて、定期実行型から常駐ストリーミング（streamable）型へアーキテクチャを刷新する際は、**「常駐型であっても、プラットフォーム側がプラグインとしてスキャンして起動するためには、ファイル名に更新間隔（時間指定）を含める規則が必須である」**という内部仕様を絶対に忘れてはならない。
これを怠って単に時間指定のないクリーンなファイル名（`status.py` 等）で配置すると、プラットフォームから完全に無視されて起動すらされなくなる。
デプロイ設計時には、**「ファイル名にはフォールバック用の時間指定（`5s` 等）を維持し、内部メタデータで常駐ストリーミングをONにし、過去の無効ファイル（`status.py` や `1s.py`）をすべて自動削除クリーンアップする」**という多重防護デプロイロジックの構築が極めて重要である。

## 22. メニューバー溢れによる表示制限バグの回避と、SwiftBar 終了後の plist 強制書き換えシーケンス

### 事象
`VisibleCC` フラグを `1` にしたにもかかわらず、メニューバー上にプラグインが表示されない、あるいは一度表示されても再び消えてしまう現象が発生した。

### 原因
1. **メニューバータイトルの極限超過**: 全モデルのクォータ詳細を横並びで表示した結果、文字数が 100文字を超えて長すぎたため、macOSのメニューバー領域の物理的制限によって非表示（見切れ）扱いになっていた。
2. **SwiftBar終了時のplist上書き**: SwiftBarプロセスが生きている状態で `defaults write` を実行すると、その後の `killall SwiftBar`（アプリ終了処理）時に、SwiftBarがメモリ内に保持していた「古い非表示状態（`VisibleCC = 0`）」を再び plist に上書き保存してしまい、設定変更が打ち消されていた。

### 対策
1. **メニューバータイトルのスリム化＆優先度制御（スマートタイトル）**:
   メニューバーのタイトルを「現在の状態 ＋ 代表モデルの残量」または「警告モデル（残量20%以下）が検出された場合はその警告」のみに極限まで短縮化（文字数を1/5にスリム化）。詳細クォータはすべてドロップダウン内に美しく格納する。
2. **完全シャットダウン後の plist 書き込み**:
   必ず `killall SwiftBar` を実行して **アプリが完全に終了した状態であることを保証してから** `defaults write` を行い、その後に `open -a SwiftBar` でアプリを再起動させるシーケンスを徹底する。

### 教訓
メニューバーウィジェットの開発においては、**タイトル文字列を絶対に長大にしてはならず、常時20〜30文字以内に抑えること**が、OSによる非表示バグを回避する最も堅牢なアプローチである。また、`defaults` コマンドでアプリの設定を直接改変する際は、**アプリの完全終了 ➔ defaults実行 ➔ アプリ起動** という順序関係を厳密に守ることで、設定の上書き打消しバグを100%防ぐことができる。

## 23. ワークスペースなし起動時の VS Code 拡張機能アクティベーション停止問題と、再インストール時のキャッシュバグの完全回避

### 事象
V1.5.0 を VSIX から再インストールし、ウィンドウをリロードしても拡張機能が起動せず、SwiftBar へのスクリプトデプロイやリフレッシュ再起動が一切動かない不具合が発生した。

### 原因
1. **ワークスペース未選択時のアクティベート制限**: `package.json` で指定していた `"activationEvents": ["onStartupFinished"]` は、VS Code がフォルダ（ワークスペース）を開かずに起動された場合、環境やバージョンによっては発火が著しく遅れる、または発火自体が停止してしまうバグが存在する。
2. **VS Code の VSIX 同一バージョン・キャッシュバグ**: VS Code は同じバージョン番号の VSIX パッケージを重ねて再インストールした際、物理ファイルのロードを完全に再読み込みせず、メモリ内の古いキャッシュや古いビルドファイルのモジュールをそのまま掴み続けてしまう致命的な不具合がある。このため、修正が正しく反映されない現象が発生する。

### 対策
1. **常時アクティベート仕様（`"*"`）への堅牢化**:
   `package.json` の `"activationEvents"` を `"*"` に書き換えることで、ワークスペースが一切存在しない状態での VS Code 起動でも、100% 確実に最優先でアクティベーションが発火し、SwiftBar との物理連携処理が即時に起動することを保証する。
2. **キャッシュを完全破棄させるためのバージョンバンプ（`1.5.1`）**:
   バージョン番号を **`1.5.1`** に引き上げて VSIX パッケージを再ビルドすることで、VS Code に「新規のアップデート拡張機能」として物理的にキャッシュを完全破棄させ、確実に最新の修正版コード（スリムタイトル ＆ defaults処理）を完璧に適用させる。

### 教訓
開発段階やバグ修正段階において「同一バージョンでのインストール確認」を繰り返すと、VS Code の内部キャッシュバグにより「修正したはずのコードが実際には反映されていない」という深刻な誤認・迷宮入りに陥るリスクが極めて高い。VS Code 拡張機能の重大な修正やアクティベーションイベントの調整を行った際は、**「絶対にバージョンを1つ以上バンプしてビルドすること（例: 1.5.1）」** および **「ワークスペース未選択状態でも確実に動かすために activationEvents を * にすること」** を徹底することが、デプロイバグを100%防止し一撃で確実な復帰を勝ち取るための鉄則である。





## 24. 【絶対禁止】勝手なバックデート（古い状態への巻き戻り）の防止と修正の明文化

### 事象
VSCode拡張機能のパッケージ（VSIX）をインストールする前に、ユーザーがVSCode上の有効化/無効化ボタンを操作した結果、VSCode内にキャッシュされていた古いバージョンのスクリプトがSwiftBarへ展開され、最新の修正（アニメーションの変更など）が失われるバックデート（巻き戻り）が発生した。

### 原因
AI側が「ファイルのコピー・デプロイ」のみで満足し、ユーザーの環境（VSCodeにインストールされている拡張機能自体）を最新版に更新する案内や手続きを怠ったため、ユーザーの通常の操作（ボタン操作）によって単一障害点（SSOT）ではない古いキャッシュが呼び出されてしまった。

### 対策・ルール（絶対命令）
1. **バックデートの絶対禁止**: 変更を加えた際は、システム全体の「単一の真実の情報源（SSOT）」が完全に最新化されていることを保証し、ユーザーの任意の操作によって古い状態に巻き戻るリスクを完全に排除しなければならない。
2. **変更点の明文化（必須）**: 追加・修正・変更した部分については、**必ず箇条書きで具体的にユーザーへ報告すること**。黙って裏で変更してはならない。（今作っている部分のデバッグ・修正を除く）
3. **デプロイパスの完全性**: 拡張機能などを更新した場合は、必ず `.vsix` 等のインストールまでを完了させ、古いバージョンがどこにも残っていない状態にする。

## 25. メタデータファイルの永続性とログ末尾走査によるステート検出の優位性

### 事象
AIの会話フォルダに生成される `.metadata.json` の `requestFeedback: true` をチェックして「承認待ち（💬😐）」を判定していたところ、過去に一度でも承認待ちが発生した会話フォルダでは永久にそのフラグが残り続けるため、常に承認待ちアイコンが表示されるバグが発生した。

### 原因
AIシステムのメタデータファイルは一度生成されると書き換えられない（不変）仕様であり、現在のリアルタイムなステート（アイドルか、待機中か）を反映するものではなかったため。

### 対策
AIが出力する生の会話ログ (`transcript.jsonl`) を後ろから数千行読み込み、最後に「承認待ちリクエスト (`ask_question` 等)」が呼ばれた後、まだ「ユーザー入力 (`USER_INPUT`)」が存在していない場合のみを真の「承認待ち」と判定するロジックに完全移行した。

### 教訓
外部システムのステートを監視する際、一度書き込まれたら更新されない可能性のあるメタデータファイルに依存すると、状態の「解除」をフックできず永久にスタックする。常にログ末尾のアクション履歴から、対になるイベント（リクエストとユーザーレスポンス）の時系列を逆算して「真の現在状態」を動的算出する設計が、最も堅牢で確実である。

## 26. IDE拡張機能の重複デプロイとSwiftBarプラグインの二重起動の罠

### 事象
SwiftBarのメニューバーアイコンが2つ同時に表示（二重起動）されてしまう現象が発生した。

### 原因
手動で修正した新しいスクリプトを `antigravity_status.1s.py` としてSwiftBarプラグインフォルダにコピーした一方で、ユーザーのIDE（Antigravity IDE）にインストールされている旧バージョンの実体拡張機能 (`v1.5.3`) がIDE再起動のたびに独自のスクリプトを生成し続けていたため、2つのファイルが同時にロード・実行されていた。

### 対策
ユーザーのIDE拡張機能の実体パス (`~/.antigravity-ide/extensions/ma-do-ka.antigravity-quota-1.5.3`) 内のソースコードを直接最新のものに上書き・強制パッチ当てし、その後SwiftBar側のプラグインディレクトリ内にある過去の全スクリプト (`*.py`, `*.js`) を一掃した上で、最新版一つのみを配置・再起動した。

### 教訓
VSIXパッケージなどを使用してユーザー環境にデプロイする常駐ツールを修正する際、部分的なファイル上書きや一時的な配置だけでは、ツールの親（IDEなど）が元の古いソースを再生成してしまい、バージョン競合や二重起動の深刻なバグを引き起こす。必ず「ツールの大元（IDEにインストールされた拡張機能の実体パス）」を特定し、そこを根本的に最新化する（Single Source of Truthの維持）運用を徹底しなければならない。

## 27. 【最重要・絶対命令】README.md はプロダクトの売りであり、保護対象ドキュメントである

### ルール（例外なし）

1. **README.md の既存内容は、ユーザーからの明示的な指示がない限り、いかなる理由でも絶対に変更・削除・書き換えしてはならない。** README はプロダクトの「売り」そのものであり、容易に崩してはならない。
2. **ユーザーから変更指示があった場合でも、AI は即座に完全停止し、変更内容の詳細（何を追加/修正/削除するか）を箇条書きで提示して確認を促すこと。** 「回答待ちシークエンス」ではなく、AI 側は一切のツール呼び出し・ファイル操作を行わず完全に停止する。
3. **ユーザーが明示的に「OK」「進めて」等の承認を返した後にのみ、実際の変更作業を開始すること。**
4. このルールは README.md に対するすべての操作（追記・修正・削除・リフォーマット・セクション移動）に適用される。

### 適用範囲
- `/Users/user/Desktop/AntigravityQuarterViewer/README.md`
- 今後作成されるプロダクト公開用の README 全般


## 28. 非同期キャッシュフェッチ時のユーザー設定維持バグ

### 事象
言語設定を「日本語」に変更しても、バックグラウンドでの非同期フェッチ（`do_bg_fetch`）が走った直後（5〜15秒後）に、強制的に「英語」に戻ってしまう不具合が発生した。

### 原因
非同期でAPIデータを取得して `quota_cache.json` に保存する際、言語設定キー `"language": "en"` を常にハードコードして上書きしていたため、UI操作で書き換えられた値が非同期プロセス側で消去されてしまっていた。

### 対策
キャッシュ書き込みを行う前に、既存のキャッシュから `language` 設定をロードして引き継ぐ（マージする）ように修正した。
```python
cache_data = load_quota_cache_data()
lang = cache_data.get("language", "en")
save_quota_cache_data({
    ...,
    "language": lang
})
```

### 教訓
非同期タスクや別プロセスで共通のキャッシュ・設定ファイルを更新する際は、一部のフィールドだけを更新する場合であっても、絶対に他の既存フィールド（特にユーザーの設定値）をデフォルト値やハードコード値で破壊的に上書きしてはならない。必ず既存データをロードして引き継ぐか、差分マージ（`dict.update`等）を行う設計を徹底すること。

## 29. モックフラグの残存と is_cached の動的判定

### 事象
APIから最新データが正常にリアルタイム同期されているにもかかわらず、メニューバーやドロップダウン表示に永久に `(cached)` や `（キャッシュ表示中）` という表示が残り続けていた。

### 原因
開発中のデバッグや一時的な動作保証のためにメインループで `is_cached = True` という静的なフラグがハードコードされたまま本番実装に残っていたため。

### 対策
最後にバックグラウンドフェッチが成功した時刻（`last_fetch_time`）のタイムスタンプと、現在時刻の差分を動的に算出し、`interval + 5秒` 以内（＝同期タスクが順調に動いている）であれば `is_cached = False` (リアルタイム同期中)、それを超えていれば `is_cached = True` (キャッシュ表示中) と判定する動的なロジックへ修正した。
```python
is_cached = (time.time() - last_fetch) > (interval + 5)
```

### 教訓
開発やデバッグのために一時的にコード内に埋め込んだハードコード（モック用フラグ等）は、本番リリース前に必ず検出し、動的な判定式に置き換えることを徹底する。また、テストコードにおいてもこの動的な判定が正しくアサーションされるか確認するテストケースを設けること。

## 30. ユニットテスト実行時の stderr リダイレクト問題の回避

### 事象
ユニットテスト（`test_status.py`）を実行した際、テストに失敗しているにもかかわらずコンソール出力が完全に空（Stdout/Stderrともに空）で終了コードだけが1になるため、デバッグが極めて困難になる問題が発生した。

### 原因
検証対象の `antigravity_status.py` をインポートした時点で、プロダクションコード内のデバッグ用処理 `sys.stderr = open('/tmp/agq_crash.log', 'w')` が強制発火し、`unittest` がコンソールに出力するはずのエラーやトレースバックがすべてファイルへ流れてバッファリングされていたため。

### 対策
テストコード（`test_status.py`）のインポート部で、インポート中だけ `sys.stderr` を退避し、インポート直後に元のコンソール出力へ復元するガード処理を実装した。
```python
original_stderr = sys.stderr
import antigravity_status
sys.stderr = original_stderr
```

### 教訓
プロダクションコードで `sys.stderr` や `sys.stdout` のリダイレクトを伴うモジュールを単体テストする際、テストスイート自体の出力機能が損なわれるリスクがある。テストコードのセットアップ部でこれらを退避・復元するか、モックを活用してテスト出力が常にコンソールへ正常表示されるように防護設計を行うこと。



## [2026-06-09] ゾンビタスクによる Thinking 状態のスタック防止
**問題:** 過去のチャットセッションで、バグや強制終了などにより「完了」のログが書き込まれなかったタスクが残っていると、そのセッションの mtime が7日間経過するまで永久に Thinking 状態（🤔）になりっぱなしになるバグが発生。
**解決策:** `transcript.jsonl` のパース対象を「mtimeが直近1時間以内」に絞り、さらに「mtimeから15分以上更新がないタスクはタイムアウト（ゾンビ化）したとみなして無視する」ロジックを追加。無駄に過去数日分のファイルまで遡ってパースしないようにパフォーマンスも改善した。
**原則:** タイムアウトのない状態管理は必ずスタックを引き起こすため、定期的な状態リセットまたは期限切れ判定（TTL）の概念を常に組み込むこと。

## [2026-06-10] sedコマンドによる正規表現エスケープ漏れの事故とAPIフェッチ障害
**問題:** `sed -i "" "s/1.7.0/1.7.1/g"` を実行した結果、`1.7.0` の `.` が正規表現の「任意の1文字」として解釈され、IPアドレス `127.0.0.1` が `1.7.1.0.1` に誤って置換されてしまった。これによりローカルAPIへのフェッチがすべて `Errno 8 (nodename nor servname provided)` で失敗し、キャッシュが古い日付のまま更新されなくなるバグが発生。
**解決策:** 壊れたIPアドレスを `localhost` に修正すると同時に、SwiftBar環境でのプロキシ影響を排除するため `os.environ` からプロキシ変数を削除する処理を追加。
**原則:** `sed` 等でバージョン番号などドットを含む文字列を置換する際は、必ず `s/1\.7\.0/1\.7\.1/g` のようにエスケープするか、より安全なファイル置換ツールを使用すること。

## [2026-06-12] APIレスポンス変更に伴うクォータ表示のグループ化
**問題:** IDE側（Antigravity）でクォータ表示が「Gemini Models」「Claude and GPT models」のグループ単位に変更されたが、ステータスバースクリプトは依然として個別モデル名でパースしようとしていたため、表示が冗長になり、正確な限界値が伝わりにくくなっていた。
**解決策:** APIが返す全モデルのリストの中から、名前に "Gemini" を含むもの、"Claude" または "GPT" を含むものをそれぞれグループ化し、その中で最も「残り割合の少ない（厳しい）値」を代表値として抽出・表示するようにパースロジックとUI描画ロジックを刷新した。
**原則:** 外部APIや公式UIの表示方針が変わった場合は、古いキー・バリューのマッピングを維持するのではなく、新しいUIのコンセプト（今回は「グループ化」）に合わせてスクリプト側の抽象度も変更すること。

## [2026-06-12] Pillowによる円形プログレスリング動的画像生成とフォールバック設計
**問題:** SwiftBarのプルダウンメニュー内はプレーンテキストであり、美しい円形プログレスリングをそのまま描画することができない。また、ユーザー環境に画像ライブラリ `Pillow` がインストールされていないと、スクリプト実行エラーが発生する。
**解決策:**
1. スクリプト実行時に `Pillow` (PIL) の `ImageDraw.arc` を用いて、メモリ上に円形リング画像（%テキスト入り）を動的生成し、PNGバイトデータをBase64エンコードして `image=` パラメータでSwiftBarに渡す設計を導入。
2. スクリプト先頭でインポートエラーをトラップし、`Pillow` がインストールされていない環境では自動的にテキストによるプログレスバー（`████░░` など）にフォールバックする「安全設計」を実装。
3. リセット日時は、残量が100%未満のときのみ `MM/DD HH:MM`（日本時間）で表示し、100%満タンの場合は `-` としてユーザーの余計な混乱を防止。
4. あまり機能していなかったマンスリー上限値（分母）を廃止し、利用可能クレジット数（`Prompt: 49,500` など）のみの極めてシンプルな表記に簡素化。
**原則:** プレーンテキストUI of 限界を超えるために動的画像生成（Base64埋め込み）を行う場合、依存ライブラリの欠損（ImportError）によるプログラム停止を絶対に避けるため、**「非破壊的なテキストフォールバック（自動退避ロジック）」**を常に最初から組み込んでおくこと。また、満タン時は時間表示を省略するなど、ノイズを排除した情報アライメントを徹底すること。

## [2026-06-12] 承認待ち (Pending) 状態検知の不足とロジック改善
**問題:** エージェントがツール（例: `run_command` や `write_to_file` 等）を提案してユーザーの承認（Proceed）を待っている状態の時、メニューバー上のステータス表示が `🧠 Thinking` から `⚠️ Pending!` にならず、通常（👾）や `Thinking` のままスタックしてしまう問題が発生。原因は、`check_pending_approval()` が `ask_question` と特定のアーティファクト承認しか検知しておらず、また過去の実行済みログまで遡って誤検知を繰り返していたため。
**解決策:** 承認が必要なツール名とその実行結果ログの `type`（例: `RUN_COMMAND`）をマッピングし、`transcript.jsonl` を逆順走査して最新の `PLANNER_RESPONSE` に到達するまでの間に結果ログが出力されていないツールコールがあるか（＝未実行の承認待ちがあるか）を判定するロジックに修正。さらに最新の `PLANNER_RESPONSE` をパースし終えた時点で探索を打ち切ることで誤検知を防止。
**原則:** エージェントの自律稼働や対話システムにおいて、「ユーザーの入力を待つ」承認待ち（Proceedなど）は対話ツールの枠を超えて発生するため、実行結果ログの有無と照合して動的に状態判定をすること。また、ログスキャン時は直近の最新応答に到達した時点で判定を確定させ、過去ログのゴミデータによる誤作動を防ぐこと。

## [2026-06-12] デプロイ対象スクリプトの配置パス誤認と、SwiftBarプラグインディレクトリの不一致によるバージョン・表示の更新漏れ
**問題:** 開発対象のスクリプトをデーモンフォルダ（`~/.gemini/antigravity/daemon/`）にのみコピーデプロイしたため、実際に SwiftBar アプリがロードしていたプラグインディレクトリ（`/Users/user/Library/Application Support/SwiftBar/plugins/`）側の実スクリプトが更新されず、メニューバー上の表示形式やバージョン表記が古いまま（v1.7.1）になる不整合が発生した。
**解決策:**
1. `defaults read com.ameba.SwiftBar` コマンドを使用して SwiftBar が実際にロードしている `PluginDirectory` の設定値を取得し、正しい配置パスを特定。
2. 修正したスクリプト（`antigravity_status.py`）をデーモン用ディレクトリに加えて、特定された SwiftBar プラグインディレクトリ側にも上書きコピーして適用。その後、SwiftBar プロセスを再起動して反映した。
**原則:** 外部アプリケーション（SwiftBar や Alfred 等）と連携するスクリプトをデプロイする際は、環境設定（plist など）やプロセスが実際に読み込んでいるプラグインディレクトリを確実に調査すること。一見関連しそうなフォルダ（daemonなど）にのみ配置するのではなく、**「実際にシステム上で動作しているプラグインの実行パス」**に確実にコピーして上書きデプロイを行うこと。

## [2026-06-12] macOS Retina (HiDPI) 環境における Base64 動的画像のぼやけ（にじみ）問題と、2倍サイズ＋DPIメタデータ埋め込みによる高精細レンダリング設計
**問題:** Retina ディスプレイ環境で、標準の 1x 画像（360x64px）をそのまま Base64 エンコードして `image=` パラメータで SwiftBar に出力すると、OS 側で拡大描画されてしまい、文字や円形プログレスリングがぼやけて（にじんで）表示される。
**解決策:**
1. 画像のキャンバスサイズおよび描画対象（文字フォントサイズや描画座標など）をすべて2倍のサイズ（720x128px）にスケールアップして高解像度で生成する。
2. 画像の保存時に、DPI情報メタデータとして `(144, 144)` を指定して PNG 保存する。
   `image.save(buffer, format="PNG", dpi=(144, 144))`
3. これにより、base64 を受け取った macOS の `NSImage` が「解像度 144 DPI（2x）の画像」として自動認識し、メニューバー内では元の 360x64 ポイントの表示枠の中にピクセルパーフェクトで縮小描画するため、にじみのない極めてくっきりとした表示を実現できた。
**原則:** macOS の Retina 環境に対応した動的画像を Base64 形式で外部連携（SwiftBar など）に渡す際は、**「キャンバスサイズと描画フォント・座標をすべて2倍で描き、PNG保存時に明示的に `dpi=(144, 144)` の解像度メタデータを埋め込む」**こと。これにより、OS 側の高解像度（@2x）縮小レンダリングがトリガーされ、ぼやけやにじみを完全に解消できる。

## [2026-06-13] 単体テスト・連結テスト・表示の総合テストの自律的計画および徹底検証ルール
**問題:** 開発プロセスにおいて、テストを明示的に行わずにリリースやデプロイを行ってしまうと、Y軸目盛りの重複や表示アニメーションの不整合、競合といったバグが本番環境で発生してしまう。
**解決策:** 今後のいかなる変更やバグ修正においても、AI（開発エージェント）自身で以下の「3段階の検証・テスト」を必ず自律的に計画し、実行し、その結果の正当性を証明するまでタスクを完了と見なさないルールを徹底する。
1. **単体テスト (Unit Test)**: 新規および変更された関数・ロジックの入出力に対する検証。特に境界値（例: 0%や100%のケース、差が極小のケース、計算式でゼロ除算が発生しうるケース）のエッジケースに対するテストを個別にコードレベルで実行する。
2. **連結テスト (Integration Test)**: モジュールやコンポーネントを結合した状態での検証。データ連携（APIから取得した構造がUIまで欠損なく渡り、正しくハンドリングされるか）のデータフローを検証する。
3. **表示の総合テスト (Visual & E2E Test)**: 実際の表示上のクオリティ検証。ホバー時のチラつき、フォントや描画のにじみ、目盛り重複、座標の追従や画面サイズへのフィットなどを、E2Eテストやシミュレーション（ダミーデータの差し込み表示など）を用いて目視および検証スクリプトによって総合的かつ徹底的にテストする。
**原則:** 「動作検証を行わずに提出しない」ことを絶対の掟とする。テスト手法や計画（単体/連結/総合の3種）を開発前に `task.md` または `implementation_plan.md` に明記し、すべての検証結果ログやキャプチャで安全を証明した後に完了とすること。

## [2026-06-13] エージェント状態の判定優先順位による「Working」表示のスタックバグ修正
**問題:** エージェントがバックグラウンドタスクを実行中（Working状態）の時、LSPプロセスが一時的に停止している（あるいは検知できない）場合、メニューバー表示が `🧠 Thinking` にならず、`⚪️ 停止中`（offline）になってしまう問題が発生。原因は、`detect_agent_state` 関数において、LSPプロセスのアクティブ状態 (`active = False`) のチェックを最優先に行い、有無を言わさず `offline` を返していたため。
**解決策:** `detect_agent_state` 内の判定順序を修正し、`is_pending`（承認待ち）や `status.get("is_thinking", False)`（タスク実行中/思考中）の判定を `active` チェックよりも優先するように変更。これにより、LSPプロセスが死んでいても、何らかの処理が動いている間は確実に `thinking`（＝稼働状態）として表示されるように改善した。
**原則:** プロセスの生存判定（LSPプロセスなど）はあくまでシステム的な一側面にすぎない。バックグラウンドタスクの稼働情報や対話ステータスなど、「実際に処理が動いているか」を判定できる他のソースがある場合はそちらを優先し、システム全体の稼働状態（ユーザーから見たWorking状態）と同期させるべきである。

## [2026-06-13] 状態判定ループにおける探索終了条件と実行結果ログ型の不一致による承認待ち（Pending）スタックバグ
**問題:** すべてのアクション（Proceedなど）が完了して返答が終わっているのにもかかわらず、メニューバー表示が `⚠️ Pending!`（承認待ち）のままスタックして解除されないバグが発生。原因は、
1. テキスト返答などの `tool_calls` キーを含まない `PLANNER_RESPONSE` ログにおいて、終了条件判定をスルーし、過去の古いターン（未実行だった別の会話など）まで探索を遡って誤判定していた。
2. システムから出力されるツールアクション実行結果ログの型 `CODE_ACTION` が、スクリプト内の `REQUIRED_APPROVAL_TOOLS`（`REPLACE_FILE_CONTENT`等）と合致せず、実行済みのツールが「未実行」とみなされていた。
**解決策:** 逆順走査時に最新のモデル応答（`PLANNER_RESPONSE`）に到達した時点で、`tool_calls` の有無にかかわらず、その回の応答だけを評価して即時に探索を終了させるように修正。また、`CODE_ACTION` 検出時にファイル編集系を完了として記録する処理を追加した。
**原則:** 時系列のログを遡って状態判定を行う場合、各ターンの確定ポイント（モデルの応答など）に到達した時点で即座にそのターンの評価を終了させ、過去の無関係なゴミデータを誤検知しない設計を徹底すること。また、実行結果ログの型（CODE_ACTIONなど）はプラットフォームによって動的に変化しうるため、実ログと突き合わせて正しく照合すること。

## [2026-06-13] 開発版の修正スクリプトを現在動いているシステムバーおよびIDE実体ディレクトリへ適用する即時デプロイ手順
**問題:** バグ修正やバージョンバンプをコードベース上で行っても、現在動作している SwiftBar プラグインおよび IDE にインストールされた実体拡張機能ディレクトリのファイルが古いままでは、IDE再起動時に古いファイルが配置されたり、動作環境に不具合が残ったりする。
**解決策:** 
1. `defaults read com.ameba.SwiftBar PluginDirectory` で取得した SwiftBar プラグイン用ディレクトリにある `antigravity_status.2s.py` を最新のスクリプトで直接上書きする。
2. IDEの拡張機能実体フォルダ（例: `~/.antigravity-ide/extensions/ma-do-ka.antigravity-quota-2.0.0/`）内の関連ファイルを最新版で直接上書きコピーする。
3. `killall SwiftBar && open -a SwiftBar` を実行してプラグインを強制再読み込みさせる。
**原則:** ツールのアシスト開発時、リリースをVSIX等で行うだけでなく、**「今現在ユーザー環境で動いているアクティブなプロセスおよびロード元（実体拡張フォルダ、プラグインフォルダ等）」**を特定して直接最新ファイルを配置・適用し、依存する連携プロセス（SwiftBarなど）を再起動することで、手動再起動等の負担を掛けずに即時反映を完了すること。

## [2026-06-13] 会話ログの更新日時 (mtime) と最終行の応答評価による「Working（自律動作中）」状態の動的検知
**問題:** エージェントがファイルの探索・編集・長考などの自律動作を行っている最中（画面上はWorking状態）、LSPへのAPIリクエストが走っておらずLSPプロセス自体も一時的に無効/検知不能になるため、メニューバー表示が `⚪️ 停止中`（offline）や `待機中` になってしまう問題が発生。
**解決策:** 最も新しく動いた会話ログ `transcript.jsonl` の最終更新日時（mtime）が 120秒（2分）以内であり、かつ最終行が「ツールコールを含まないモデル応答（PLANNER_RESPONSE）」ではない（＝まだ最終返答を返しておらず自律動作を継続している）場合を、LSPプロセス状態にかかわらず「稼働中（Thinking）」と動的に判定するロジックを実装。
**原則:** アプリケーション側の内部プロセス（LSPなど）の生存状況だけを動作判定の唯一의ソースにすると、エージェントが自律的に動いている（LSPと非同期に動く）際の「Working」状態と矛盾してしまう。対象システムが動作することによって必ず更新される共有情報（会話ログのmtimeやステップ種別など）を判定ソースとして併用し、ユーザーの見ている進行状態（Working）とメニューバー表示を確実に同期させること。

## [2026-06-13] 承認待ちログ（BLOCKED/PENDING）の誤認によるメニューバースタックバグの修正
**問題:** コマンド実行等の承認待ちダイアログが表示されている最中に、メニューバー表示が `⚠️ Pending!`（承認待ち）にならずに `🧠 Thinking`（ただの動作中）のままスタックしてしまう問題が発生。原因は、`check_pending_approval` 関数において、`RUN_COMMAND` などのログレコードが検出された際、その `status`（`BLOCKED` や `PENDING` など）を考慮せず無条件で「実行済み」としてリストに追加してしまっていたため。結果として、直前にある `PLANNER_RESPONSE` で承認待ちコマンドが提案されていても「すでに実行が完了した」と誤認され、Pending 状態がスルーされていた。
**解決策:** 逆順走査時に `RUN_COMMAND` 等の対象アクションレコードを見つけた際、その `status` が `"BLOCKED"`、`"PENDING"`、`"WAITING"` のいずれか（＝未承認の保留状態）であれば即座に承認待ち（`return True`）と判定し、それ以外の実行開始・完了ステータスの場合のみ「実行済み（`executed_types`）」に追加するように修正した。
**原則:** 時系列の操作ログをスキャンして状態検知（承認待ちや実行中など）を行う場合、ログレコードの有無（タイプ）だけでなく、「その操作がどのような状態にあるか」を示す内部ステータス（status値）まで精緻に評価して状態遷移を管理すること。単にログが書き出されていることだけをもって「実行済み」と断定してはならない。

## [2026-06-15] 頻繁なプロセス起動・描画更新による WindowServer の Machポート枯渇クラッシュの防止
**問題:** SwiftBar 関連プロセス（メニューバー更新スクリプト）の動作周期が `2s`（2秒更新）と非常に短かったため、プロセスの新規起動・終了、Base64画像のデコード、CGImageへの描画、CoreAnimationトランザクションコミットが2秒ごとに繰り返された。これにより macOS の `WindowServer` プロセス側で XPC や Machポート（通信リソース）が蓄積（リソースリーク）し、許容上限（約30万個）を超えてカーネルによる SIGKILL（強制終了）を招き、結果としてユーザーの macOS セッションが強制ログアウトされる深刻なインシデントが発生した。
**解決策:** プラグインの動作周期を `2s` から **10s（10秒更新：`antigravity_status.10s.py`）** に引き下げた。これによりプロセスの起動回数および描画トランザクション負荷を 80% 削減した。また、過去の無駄な古いプロセスやファイルを自動クリーンアップし、リーク速度を安全圏（通常の使用ではクラッシュしなくなるレベル）に抑え込んだ。
**原則:** macOS のメニューバーやウィジェットの更新を自動で行う際、アニメーションやリアルタイム性を追求するあまり、2秒以下の極端な高頻度でのプロセス起動や画像描画更新を設定してはならない。OS側（WindowServer等）がリークを引き起こすリスクがあるため、10秒〜30秒などの常識的な時間枠に周期を抑え、リソース負荷の増大（特に Machポート等の累積）を抑制する安全設計を徹底すること。

## [2026-06-15] メニューバータイトルのテキスト化 ＆ プルダウン詳細画像限定による WindowServer リークの完全防止と 2秒周期の安全な復旧 (v2.2.0)
**問題:** 以前の対策として更新周期を10秒に緩和したものの、依然として画像の描画更新がある限り、低頻度ながらも WindowServer 側の Mach ポートはリークし続ける懸念があった。また、10秒周期ではエージェントの状態（Thinking/Pending）の連動が遅れてしまい、リアルタイム性が損なわれていた。
**解決策:** 
1. メニューバーに常時表示される「タイトル」部分から Pillow による画像生成・セット処理を完全に排除し、テキストと Unicode ドットゲージのみで構成するように変更した。
2. 美しい円形プログレスリング画像（Pillow 動的描画）は、ユーザーがメニューをクリックして開いた時の「プルダウン詳細項目」内にのみ出力するように限定した。
3. メニューを閉じている通常監視時（99.9%の時間）は WindowServer での画像描画・デコードが発生しないため、Mach ポートのリークおよび描画負荷は完全にゼロになる。これにより、プラグインの動作更新周期を再び安全に **2秒** (`antigravity_status.2s.py`) に戻し、エージェント稼働状態のほぼリアルタイムな追従を復旧した。
**原則:** ステータスバーやウィジェットなどで画像を動的に生成・表示する場合、画面上に常時露出する「タイトル（常時描画領域）」に高頻度で画像をセットし続けると、OS のウィンドウマネージャー（WindowServerなど）でリソース枯渇を引き起こす。常時露出部分は「テキスト（ゲージ等）」に留め、画像は「クリック等で開く詳細メニュー（一時描画領域）」にのみ限定することで、優れたビジュアル品質を保ったままシステムの完全な安定性を確保できる。

## [2026-06-15] バックグラウンドフェッチの多重起動防止とロックライフサイクル設計
**問題:** API クォータ情報を取得するための非同期バックグラウンド処理（`--fetch-bg`）において、前回のフェッチ処理がタイムアウトやネットワーク遅延で完了する前に新たなフェッチがキックされると、不要なフォークが複数立ち上がるプロセス詰まり・メモリ浪費が発生し、システムの安定稼働を阻害する。
**解決策:** フェッチ処理の開始時に30秒間有効なロックファイル (`fetch.lock`) による分散ロック機構を実装。すでにロックが存在する場合は即座にフォークを終了し、フェッチ完了後または30秒経過後にロックを自動クリアしてゾンビ化を防止。
**原則:** 非同期の外部 API ポーリング処理をバックグラウンドに逃がす場合、多重実行を抑制する有効期限（TTL）付きのファイルロック機構を必ず備え、プロセス肥大化を自己防止すること。

## [2026-06-15] Pillow 動的描画時の TrueType フォントキャッシュ化によるファイル I/O 排除
**問題:** ユーザー環境に Pillow が存在する場合の円形ゲージ画像生成において、2秒の更新周期ごとに Helvetica 等の大きなフォントファイルをディスクから都度読み込んでラスタライズしていたため、微小ながら恒常的なディスク I/O 負荷が発生していた。
**解決策:** グローバルに `_FONT_CACHE` を定義し、一度読み込んだ TrueTypeFont オブジェクトをメモリ上にキャッシュして再利用するヘルパー関数を実装。無駄なファイル I/O および CPU 描画負荷を排除し、画像生成速度を約 10% 向上。
**原則:** 周期実行される画像生成やレンダリング処理では、静的なアセット（フォントや画像テンプレートなど）を毎回ディスクからリロードするのを避け、メモリキャッシュ（再利用）を用いて実行時間の安定化と I/O 削減に努めること。

## [2026-06-15] 一時ログファイルの配置における一般ユーザー権限・マルチユーザー競合の防止
**問題:** スクリプトのエラーやクラッシュ情報を `/tmp/agq_error.log` や `/tmp/agq_crash.log` に直接書き込んでいたため、同一マシン上で別ユーザーが本ツールを実行した際、ファイルの所有権の違いにより `PermissionError` でプラグインのインポート（起動）自体がクラッシュする問題が発生した。
**解決策:** ログおよびキャッシュの配置先を、各ユーザー専用のディレクトリ `~/.gemini/antigravity/daemon/` 配下へと完全に移行。書き込み先をサンドボックス化することでパーミッション衝突や一時ファイルの散らかりを根本的に防止。
**原則:** 共有領域（`/tmp` 等）へのファイル作成はパーミッション競合やクラッシュの原因になるため、実行ファイルが利用する一時・ログデータは常に実行ユーザー固有のスペース（`~`）配下で管理し、システム全体での安全な並行動作を確保すること。

## [2026-06-15] ホームディレクトリの動的展開による環境ポータビリティの確保
**問題:** アクティブな LSP ログファイルの参照パスに `/Users/user/Library/Logs/Antigravity/language_server.log` という固定のユーザー名がハードコードされていたため、前述のように別ユーザー環境や異なる macOS マシンで動作させた場合にログファイルを見失い、稼働状態 (Thinking/Pending) の検知が機能しなくなる問題が発生した。
**解決策:** パス定義部を `os.path.expanduser("~/Library/Logs/Antigravity/language_server.log")` に修正し、ホームディレクトリを動的に解決するように刷新。
**原則:** スクリプト内でのファイル読み書きにおいて、環境やユーザーによって異なる親ディレクトリパス（ホームディレクトリなど）を絶対パスでハードコードしてはならない。必ず `os.path.expanduser` 等を用いて実行時環境のパスを動的に解決し、コードのポータビリティと堅牢性を維持すること。

## [2026-06-15] GitHub Actions のステップ if 条件内における secrets 直接参照の禁止と環境変数マッピングによる再発防止
**問題:** GitHub Actions のワークフロー（`release.yml`）内のステップで `if: ${{ secrets.OVSX_PAT != '' }}` のように `secrets` を直接参照する式を設定したところ、Actions の構文解析時に `Unrecognized named-value: 'secrets'` というエラーが発生し、ワークフロー自体が無効（構文エラー）として実行前に失敗する致命的な問題が発生した。
**解決策:** ステップの `if` 条件では `secrets` コンテキストは直接利用できないため、いったん該当ステップ（またはジョブ）の `env` 経由でシークレットを環境変数にマッピングした上で、`if: env.OVSX_PAT != ''` として判定し、実行時引数にも `env` 変数（`$OVSX_PAT`）を渡すことで、構文エラーを完全回避した。
**原則:** GitHub Actions において、ステップの `if` 条件判定で認証情報（トークン等）の有無を確認する際は、**「絶対に `secrets` を `if` 内で直接参照してはならない」**。必ず `env` レベルで安全にマッピングし、環境変数の評価を通じて条件分岐と処理へのパラメータ受け渡しを行うこと。


## [2026-06-25] 推測情報表示の危険性と生データ（正確性）の徹底
**問題:** APIが「その時点で最も厳しい制限の代表値」しか返さない制約下で、片方の制限情報からもう片方の状態（100%に回復しているなど）を推測・補完して表示する個別キャッシュ設計を導入したが、実際のWeb UI上のクォータ情報（例：Weekly 24%, 5-hour 55%）と大きく乖離し、ユーザーへ不正確な情報（例：Weekly 100%, 5h 65%）を提示してしまう致命的なバグ（誤情報の提供）を引き起こした。
**解決策:** 
1. 推測による5h/Weeklyの仕分け表示を完全に廃止し、APIが返した生のクォータ代表値とリセット時間から判定される制限タイプの suffix（`[5h]` または `[Weekly]`）のみを表示する元の正しい仕様に差し戻した。
2. キャッシュに旧個別キーが存在する場合は自動的に初期化して差し戻す安全処理を追加した。
**原則:** ユーザーにとって「誤った情報になるくらいなら不要」である。APIから取得できない情報をクライアント側の論理推測で埋めるような設計は、結果として不確実性を伴い、システムの信頼性を著しく損なう危険性がある。情報が不足している場合は、推測による補完を一切行わず、APIが保証する生データ（正しい情報）のみを誠実にそのまま表示することを徹底しなければならない。
